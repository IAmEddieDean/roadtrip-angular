h3.text-center A List of your Trips
.row
  .col-xs-8.col-xs-offset-2
    table.table.table-striped
      thead
        tr
          th Name
          th Departure
          th Created At
      tbody
        tr(ng-repeat='trip in trips')
          th {{trip.name}}
          th {{trip.depart | date: medium}}
          th {{trip.createdAt | date: medium}}
          th: a.btn.btn-xs.btn-info(ui-sref='trips.show({tripId: trip._id})') Show
